{
  "name": "aiquiz-oauth",
  "version": "1.0.0",
  "description": "OAuth 2.1 Provider for aiquiz.pl platform",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit",
    "cf-typegen": "wrangler types",
    "db:migrate": "wrangler d1 migrations apply aiquiz-oauth"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.1.0",
    "@workos-inc/node": "^7.74.2",
    "jose": "^6.1.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251217.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.45.0"
  }
}
